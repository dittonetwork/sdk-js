{
	"name": "@repo/core",
	"license": "MIT",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"default": "./src/index.ts"
		}
	},
	"scripts": {
		"test": "vitest",
		"clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
	},
	"devDependencies": {
		"@ditto/tsconfig": "workspace:*"
	},
	"publishConfig": {
		"access": "public"
	}
}
